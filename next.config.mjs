/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['ap-south-1.graphassets.com'], // Ensure the string is closed and the array is properly terminated
      },
};

export default nextConfig;
